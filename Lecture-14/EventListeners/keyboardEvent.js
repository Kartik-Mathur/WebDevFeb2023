const body = document.querySelector('body');
const searchBox = document.querySelector('#searchBox');

body.addEventListener('keydown',(ev)=>{
    console.log(ev)
    // searchBox.value += ev.key;
})

